window.onload = (event) => {
    console.log('Initiating browser detection sequences.');
  };

  // Analyze browser data. Aggregate by using encryption methods. 
  // Compose data into JSON, and send it to server.
  // Validate data server-side, and send back encrypted cookie to track/verify user.
